{
  "criterion_id": "LINT-23",
  "evidence": [
    {
      "artifact": "docs/specs/move-the-pricing-arithmetic-out-of-render-into-its-own-module-with-tests.md",
      "line_start": 220,
      "line_end": 226,
      "exact_quote": "- **Case 22, the pre-change capture:** an assertion, with `assert.strictEqual`,\n  of the full HTML string that **unmodified** `render()` returns for that\n  fixture. This is the artifact Phase 3 compares against. It lives at a known\n  path in the repository rather than in someone's terminal scrollback, and\n  `node --test tests/render.characterization.test.js` re-runs the comparison on\n  demand — it is created here, in the last phase before any code moves, because\n  this is the state it exists to preserve.",
      "sha256": "8330b478aa24271d14189b066045116e8edaa644e13e548e13af0018695437ee"
    },
    {
      "artifact": "docs/specs/move-the-pricing-arithmetic-out-of-render-into-its-own-module-with-tests.md",
      "line_start": 313,
      "line_end": 315,
      "exact_quote": "- `node --test tests/render.characterization.test.js` passes unedited, so the\n  comparison is repeatable by anyone at any later date rather than resting on a\n  one-time observation.",
      "sha256": "8330b478aa24271d14189b066045116e8edaa644e13e548e13af0018695437ee"
    }
  ],
  "reasoning": "The rollback and comparison artifacts the plan creates satisfy both halves of the rule. Case 22, the pre-change capture that Phase 3 compares against, is created in Phase 1 - stated in the deliverable as the last phase before any code moves, because that is the state it exists to preserve - and it is re-runnable rather than a one-time observation: it lives at a known repository path and node --test tests/render.characterization.test.js re-runs the comparison on demand, which Phase 3 makes an exit criterion. The fixture it reads, tests/fixtures/report-real.json, is committed alongside it. package-lock.json is likewise created in Phase 0, the baseline it makes reproducible.",
  "verdict": "MET"
}
